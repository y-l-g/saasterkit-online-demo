# Implementation Plans

Generated by the improve skill on 2026-06-13 at commit `fcef216`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done
unless a reviewer says they maintain this index.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Harden signed login links for two-factor users and team redirects | P1 | S | - | DONE |
| 002 | Restore Socialite OAuth state validation on web callbacks | P1 | S | - | DONE |
| 003 | Move billing session creation routes to POST requests | P1 | M | - | DONE |
| 004 | Align the team admin role with its advertised permissions | P1 | S | - | DONE |
| 005 | Stabilize generated TypeScript types and remove machine-specific output | P2 | M | - | DONE |
| 006 | Add missing relationship constraints and prevent orphaned team billing rows | P2 | M | - | DONE |
| 007 | Resolve the high npm audit advisory for esbuild in build tooling | P2 | M | - | DONE |
| 008 | Refresh README stack details and local verification commands | P3 | S | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- Plans 001 and 002 are independent but both touch auth. If one is in progress,
  coordinate before editing shared auth tests.
- Plan 003 may require Wayfinder regeneration; keep generated route artifacts
  consistent with the repo's ignore rules.
- Plan 004 may interact with plan 005 only if type generation is broken before
  running frontend checks. If so, execute plan 005 first.
- Plan 006 is intentionally after the lower-risk app fixes because it contains
  a migration and data-cleanup risk.
- Plan 007 is dependency metadata only; do it separately from feature changes.
- Plan 008 is docs-only and can land any time.

## Verification baseline from audit

These commands passed before plans were written:

- `php artisan test --compact` - 141 tests passed.
- `npm run test:frontend` - 8 tests passed.
- `npm run types:check` - exit 0.
- `npm run lint:check` - exit 0.
- `npm run format:check` - exit 0.
- `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` - no errors.
- `vendor/bin/pint --test --format agent` - passed.
- `composer audit --no-interaction` - no advisories.

Known failing or non-clean checks before execution:

- `npm audit --audit-level=high` fails on high esbuild advisories through
  `@nuxt/ui -> @nuxt/fonts -> fontless -> esbuild`.
- `vendor/bin/rector process --dry-run --no-progress-bar` reports four files
  it would change. This is currently a separate low-priority cleanup, not a
  blocker for plans 001-008.

## Findings considered and rejected

- Broad notification scalability refactor: not planned because the immediate
  issue is lower leverage than selected auth/billing/data-integrity findings.
- Socialite provider enum route constraints: useful follow-up, but not planned
  separately because the selected high-risk issue is state validation.
- Browser/live deployment audit: out of scope for this planning pass.
